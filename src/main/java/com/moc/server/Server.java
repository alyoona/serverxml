package com.moc.server;

import com.moc.jdbcclient.executors.QueryExecutor;
import com.moc.jdbcclient.parsers.Parser;
import com.moc.jdbcclient.queries.Query;
import com.moc.jdbcclient.results.QueryResult;


public class Server {

    public void start() {
//        String sql = "CREATE DATABASE my_DB;";
//        Query query = Parser.parse(sql);
//        QueryExecutor queryExecutor = new QueryExecutor();
//        queryExecutor.process(query);
//        QueryResult queryResult = queryExecutor.getResult();
//        String result = queryResult.getResultMessage();
//        System.out.println(result);
    }
}
